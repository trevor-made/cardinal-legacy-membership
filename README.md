# cardinal-legacy-membership (redirect stub)

This public repo exists **only** to keep the old GitHub Pages URL alive as a redirect.

The project moved to the private repo **`trevor-made-sousa/cardinal-legacy-membership`** (so the
source could be private while the Pages site stays public). The canonical page is:

→ https://trevor-made-sousa.github.io/cardinal-legacy-membership/

`index.html` here is a meta-refresh + JS redirect to that URL. No strategy content lives in this repo.
